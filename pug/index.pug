| {{ partial "head.html" . }}

{{ if isset .Site.Params "mainMenu" }}
body
    .section#splash
        | {{ range .Site.Params.mainMenu }}
        #big-link
            a(href='{{.link}}') {{.text}}
        | {{ end }}
{{ end }}
